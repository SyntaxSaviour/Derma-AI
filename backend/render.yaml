services:
  - type: web
    name: derma-ai-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python download_models.py && cd src && uvicorn main:app --host 0.0.0.0 --port $PORT